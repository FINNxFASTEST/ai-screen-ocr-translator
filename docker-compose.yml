services:
  translate-model:
    provider:
      type: model
      options:
        model: ${TRANSLATE_MODEL:-docker.io/ai/gemma4:E2B}

  ocr-model:
    provider:
      type: model
      options:
        model: ${OCR_MODEL:-docker.io/ai/gemma4:4B}
