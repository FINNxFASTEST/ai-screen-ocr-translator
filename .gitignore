# AI coding assistant artifacts
.cursor/
.claude/
.copilot/

# Aider-style files
.aider*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# OCR debug dumps (when enabled)
debug/

# Local agent instructions
CLAUDE.md

# Per-machine / local overrides (effective config order: config.user.json → config.default.json → config.json)
config.user.json
config.json

